# Test-harness dependencies for running solver.py under CPython.
#
# The deployed site does NOT install these: it fetches SymPy through Pyodide in
# the browser (see index.html / PYODIDE_SRI). This file exists so the one real
# Python dependency is declared rather than implied by a README line — which is
# also what lets Dependabot see it at all.
#
# mpmath is SymPy's own dependency, pinned here because the definite-integral
# verifier calls it directly for quadrature.
sympy==1.14.0
mpmath==1.3.0
